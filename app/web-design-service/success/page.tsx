import SuccessClient from './SuccessClient';

export const metadata = {
  title: 'Order Confirmed | Webhosting NZ',
  robots: { index: false, follow: false },
};

export default function CheckoutSuccessPage() {
  return <SuccessClient />;
}
